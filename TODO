game loop
   fix negative bet when allin
   hand evaluator

implement virtual function for all events (client)
   just notifiy about changes? (single function)


read function
   add timeout
   check sanity
   own buffer per seat?


use /dev/random
