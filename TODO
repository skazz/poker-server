game loop
   hand evaluator
   pot distribution

implement virtual function for all events (client)
   just notifiy about changes? (single function)


read function
   add timeout
   check sanity
   own buffer per seat?


use /dev/random
