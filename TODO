game loop
   abort playersLeft = 1
   showdown (reveal cards)

test handEvaluator

logfile

just notifiy about changes? (single function)


read function
   add timeout
   check sanity
   own buffer per seat?


use /dev/random
